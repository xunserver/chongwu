#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
exec env HOME="$PROJECT_ROOT" "$PROJECT_ROOT/.codex/superpowers/.codex/superpowers-codex" "$@"
